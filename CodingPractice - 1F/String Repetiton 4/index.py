# You are given a string. Repeat the same string N times separated by space.

word = input() # Input: messages
number = int(input()) # Input: number of times to repeat the string
print((word + " ") * number) # Output: messages messages messages messages messages
